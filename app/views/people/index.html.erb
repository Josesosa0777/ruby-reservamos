<div class='container center-block'>
    <h1>
        People
    </h1>
    
    <table class="table table-hover table-responsive col-12 text-center">
    <thead class="thead-dark">
        <tr>
        <th width="20%">Name</th>
        <th width="15%">Category</th>
        <th width="20%">Email</th>
        <th width="15%">Area</th>
        <th width="20%">Leader</th>
        <th width="10%">More</th>
        </tr>
    </thead>
    <% @people.each do |person| %>
        <tbody>
        <tr>
          <td width="20%"><%= person.name %></td>
          <td width="15%"><%= person.category %></td>
          <td width="20%"><%= person.email %></td>
          <td width="15%"><%= person.area %></td>
          <td width="20%"><%= person.leader %></td>
          <td width="10%"><%= link_to 'Ver más', person %>
        </tr>
      </tbody>
    <% end %>
    </table>
   
    <!-- <%= render @people %> -->
    <div class="d-flex justify-content-center mt-5 col-sm-4 center-block">
    <%= link_to 'Add Person', new_person_path, class: 'btn btn-primary' %>
    </div>
</div>
